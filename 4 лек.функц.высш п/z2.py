# работа с одной переменной
# def calk1(a):
#     return a+a
# def calk2(a):
#      return a*a
# def math (op, x): #передаем значения в функцию с оператором x
#       print(op(x))
# math(calk2,5)

#  работа с двумя переменными
def calk1(a,b):
    return a+b
def calk2(a,b):
     return a*b
def math (op, x,y): #передаем значения в функцию с оператором x
      print(op(x,y))
math(calk2,5,45)
